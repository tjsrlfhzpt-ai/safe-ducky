# SAFE DUCKY v5.36 업데이트 보고서

작성일: 2026-09-13
DB_VERSION: 39 (변경 없음 — 저장 데이터 구조 그대로)

---

## 1. 먼저: 보내주신 진단이 v5.34의 조치가 틀렸음을 증명했습니다

보내주신 v5.35 진단 결과를 v5.33 결과와 나란히 놓으면 답이 나옵니다.

| 시험 | 파일 MIME | v5.33 진단 (안드로이드) | v5.35 진단 (윈도우 크롬 153) |
|---|---|---|---|
| 텍스트 공유 | — | 성공 | 성공 |
| .txt | v5.33 `text/plain` → v5.34 `application/octet-stream` | 성공 | **실패** |
| .csv | v5.33 `text/csv` → v5.34 `application/vnd.ms-excel` | 성공 | **실패** |
| .html | v5.33 `text/html` → v5.34 `application/octet-stream` | 성공 | **실패** |
| .png | `image/png` (안 바꿈) | — | 성공 |
| .xlsx | 원래 MIME | 실패 | 실패 |

**확장자는 그대로인데 MIME만 바꾼 것이 전부 막혔고, MIME을 바꾸지 않은 것만 통과했습니다.**

크로미움에는 확장자 검사(`IsDangerousFilename`) 말고 **MIME 검사(`IsDangerousMimeType`)가 따로** 있고, 그 허용 목록에 `application/octet-stream`과 `application/vnd.ms-excel`은 없습니다. v5.33에서 제가 "판정 기준은 확장자뿐"이라고 적은 것이 절반만 맞았고, 그 잘못된 전제 위에서 v5.34가 MIME을 바꾸는 바람에 **그때까지 잘 되던 CSV·HTML·백업(.txt) 공유까지 함께 막아버렸습니다.** 제가 만든 회귀입니다.

`navigator.canShare()`는 이 MIME 검사도 하지 않기 때문에, v5.34에 넣어둔 "거부되면 원래 MIME으로 재시도" 안전장치가 작동조차 하지 못했습니다.

## 2. 실제 구현

### ① MIME 바꿔치기 완전 폐기 (회귀 수정)

`SHARE_FILE_MIME_OVERRIDE`를 삭제하고, 대신 두 가지를 두었습니다.

| 새 구성요소 | 역할 |
|---|---|
| `SHARE_MIME_BY_EXT` | 확장자별로 확실히 허용되는 MIME (.csv→text/csv, .pdf→application/pdf 등). 공유 File은 항상 이 값으로 만듭니다 |
| `SHARE_PERMITTED_MIME_TYPES` + `isSharePermittedType()` | 크로미움 허용 MIME 목록. `canShare()`를 믿지 않고 우리가 직접 판정합니다 |

이제 CSV·HTML·백업(.txt) 공유는 v5.33 시점의 정상 동작으로 돌아갑니다.

### ② 📕 PDF로 공유 신설 — 카카오톡에 파일로 갈 가능성이 가장 높은 형식

`.pdf`는 **확장자 목록과 MIME 목록 양쪽에 모두** 들어 있는 데다, `text/`도 `image/`도 아니어서 받는 앱이 "글자"나 "사진"이 아닌 **첨부파일**로 다룹니다. 카카오톡이 CSV를 텍스트로 풀어버리는 문제를 정면으로 피해가는 형식입니다.

한글 PDF의 걸림돌은 글꼴 임베딩인데, 이번 방식은 **글자를 PDF 텍스트로 쓰지 않고 이미 렌더링된 화면을 JPEG로 담기** 때문에 글꼴 파일이 전혀 필요 없습니다. 외부 라이브러리 추가 0KB이고, 어떤 기기에서도 한글이 깨지지 않습니다. (대신 PDF 안의 글자는 그림이라 검색·복사는 안 됩니다.)

A4 여러 쪽으로 자동 분할되며, **표의 줄이 쪽 경계에서 반으로 잘리지 않도록** 렌더링할 때 각 줄의 경계 위치를 미리 재두고 그 지점에서 쪽을 나눕니다.

### ③ 공유 진단에 결정적 시험 2개 추가

| 번호 | 내용 |
|---|---|
| 7) .pdf 문서 공유 | 실제 PDF 생성 경로를 그대로 시험 |
| 8) MIME 대조시험 | 확장자는 `.csv` 그대로, MIME만 v5.34가 쓰던 값으로 보냄 |

**4)는 되는데 8)만 실패하면** 위 1번의 결론이 실기기에서도 확정됩니다.

진단 환경정보에 "**⚠ file:// 로 연 상태**" 경고도 추가했습니다 — 이번에 휴대폰이 아니라 PC에서 파일을 직접 열어 진단하셨습니다.

### ④ 기타

- PNG와 PDF를 화면 한 번만 그려서 둘 다 뽑도록 정리(`renderDocToCanvas`)
- 공유 모달 안내문에 형식별 용도 명시(원본 필요 → 엑셀 그대로 / 카톡 파일 → PDF / 대화창에 바로 보이게 → 이미지)

## 3. 실제로 수행한 테스트

| 테스트 | 결과 |
|---|---|
| 전체 스크립트 구문 검사 | 통과 |
| PDF 생성 → poppler로 검사 | 통과 (A4 595×842pt, 1쪽, DCTDecode 포함, 한글 깨짐 없음) |
| 160줄 대량 데이터 PDF | 통과 (7쪽 분할) |
| 쪽 경계에서 줄이 잘리는지 | 통과 (1쪽이 20번 줄로 끝나고 2쪽이 21번 줄로 시작 — 이미지로 확인) |
| MIME 판정 함수 | 통과 (.csv→text/csv, .pdf→application/pdf / octet-stream·vnd.ms-excel = 거부) |
| 공유 버튼 상태 3종(전부 준비중 / 전부 준비됨 / PDF만 실패) | 통과 |
| v5.35 기능 회귀(HTML 내장 엑셀, PNG) | 통과 (원본 7,451바이트 그대로) |

## 4. 확인하지 못한 것

- 이 세션에도 실기기가 없습니다. **카카오톡이 .pdf / .png / .html을 실제로 어떻게 표시하는지**는 확인하지 못했습니다.
- v5.35 진단은 PC의 `file://`에서 실행된 것이라, 그 결과가 휴대폰 + https 환경과 완전히 같다고 단정할 수 없습니다. 다만 v5.33 안드로이드 결과와 교차 확인되므로 MIME 결론 자체는 근거가 충분하다고 봅니다.

## 5. 배포 후 확인 부탁드립니다

**반드시 휴대폰에서, 배포된 https 주소로** 해주세요 (PC의 file://은 결과가 다릅니다).

1. 앱 버전이 **v5.36**인지 확인
2. 공유 진단에서 **4) .csv**와 **8) MIME 대조시험**을 둘 다 실행 → 4)만 성공하면 원인 확정입니다
3. **📕 PDF로 공유** → 카카오톡 앱 아이콘(맨 윗줄 바로가기 말고) → 대화방 → **파일로 도착하는지**
4. 3번이 되면 앞으로 카카오톡 전송은 PDF를, 원본 엑셀이 필요할 때는 "엑셀 그대로 공유"를 쓰시면 됩니다

## 데이터 안전성

저장 데이터 구조 변경 없음(DB_VERSION 39 유지). 이번 변경은 모두 내보내기·공유 경로에만 있고 데이터를 읽기만 합니다.
