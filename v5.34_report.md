# SAFE DUCKY v5.34 업데이트 보고서

작성일: 2026-09-13
DB_VERSION: 39 (변경 없음)

---

## 1. 신고 내용

v5.33의 "CSV로 공유"를 실기기(카카오톡, 삼성인터넷·크롬 둘 다)로 시험한 결과를 알려주셨습니다.

- 오류 메시지 없이 공유창까지는 뜸
- 그런데 카카오톡에 도착한 것은 파일도 텍스트도 아닌, **내용이 전혀 없는 빈 말풍선**(스크린샷으로 확인)
- 다운로드로도 시도했으나 안 됨

이번 신고는 v5.33에서 "확인하지 못했다"고 명시했던 부분(실기기에서 CSV 공유가 실제로 도착하는지)에 대한 첫 실기기 결과이고, 결과는 부정적이었습니다.

## 2. 원인 분석 — 확정이 아니라 "가장 근거 있는 추정"임을 먼저 밝힙니다

이번에는 확정적으로 원인을 특정하지 못했습니다. 근거를 순서대로 설명드립니다.

| 가능성 | 배제/채택 근거 |
|---|---|
| CSV 변환 코드 자체가 빈 내용을 만든다 | 배제 — `workbookToCsv`는 v5.33에서 실제 위험성평가 데이터로 45줄 생성까지 검증된 코드이고, 이번에 변경하지 않았습니다 |
| 크롬이 .csv 확장자를 막는다 | 배제 — 크로미움 허용 목록에 .csv가 있고(v5.33에서 원본 코드로 확인), 막혔다면 `canShare`/`share`가 예외를 던졌어야 하는데 이번엔 예외가 전혀 없었습니다 |
| 안드로이드 공유 인텐트 단계에서 문제가 생긴다 | **채택(추정)** — 일부 안드로이드 앱은 MIME이 `text/`로 시작하는 공유를 "텍스트 메시지 공유"로 취급해 첨부파일(EXTRA_STREAM)이 아닌 텍스트(EXTRA_TEXT)만 읽는 것으로 알려져 있습니다. 이번 공유는 텍스트를 동봉하지 않고 파일만 보냈으므로, 받는 쪽이 "텍스트도 파일도 아닌 빈 내용"으로 보이는 것과 정확히 들어맞는 유일한 설명입니다.

**분명히 말씀드릴 것: 이 세션에도 실기기가 없어 이 설명이 유일한 원인이라고 확정할 수는 없습니다.** 이번 조치가 효과가 없다면, "MIME 문제가 아니었다"는 것 자체가 다음 진단에 필요한 유의미한 새 단서가 됩니다.

## 2-1. "다운로드도 안 됩니다" 부분은 원인을 특정하지 못했습니다

기존 화면에는 CSV를 곧바로 "다운로드"할 수 있는 버튼이 없었습니다(공유 실패 시 나오는 "원본 다운로드"는 CSV가 아니라 원본 엑셀(.xlsx)을 내려받는 버튼이었습니다). 어느 다운로드를 시도하신 것인지 확인이 필요합니다. 이번 버전에서 CSV·HTML 전용 다운로드 버튼을 새로 만들었으니(3번 참고), 다음에는 이 버튼으로 시도해주시고 그래도 안 된다면 **"다운로드 자체가 안 뜬다"인지 "다운로드된 파일을 열어보니 비어 있다"인지**를 구분해서 알려주시면 원인 폭을 좁힐 수 있습니다.

## 3. 실제 구현

### ① CSV·HTML 공유의 MIME을 "text/"가 아닌 값으로 변경(추정 원인에 대한 조치)

| 형식 | 기존 MIME | 변경 후 |
|---|---|---|
| CSV 공유 | `text/csv` | `application/vnd.ms-excel` (이 브라우저가 거부하면 `text/csv`로 재시도) |
| HTML(서식 그대로) 공유 | `text/html` | `application/octet-stream` (거부 시 `text/html`로 재시도) |
| 백업(.txt로 위장) 공유 | `text/plain` | `application/octet-stream` (거부 시 `text/plain`으로 재시도) |

파일 확장자(.csv/.html/.txt)는 그대로 유지합니다 — 크롬의 공유 허용 여부 판정은 확장자만 보므로(v5.33에서 원본 코드로 확인) 영향이 없습니다. 새 MIME을 이 브라우저가 거부하면 기존 MIME으로 자동 재시도하므로, 만약 이전에 되던 환경이 있었다면 그 환경까지 깨지는 일은 없습니다.

### ② CSV·HTML 각각에 "공유"와 별개로 "다운로드" 버튼 신설

공유 인텐트 쪽에 어떤 문제가 있든(위 MIME 조치가 원인을 못 맞혔더라도) 최소한 파일은 확보할 수 있도록, 공유 모달에 "📊 CSV로 공유" 옆에 "💾 CSV 다운로드", "📄 서식 그대로 공유" 옆에 "💾 HTML 다운로드"를 추가했습니다. 이 다운로드는 공유 인텐트를 전혀 거치지 않고, 이 앱이 처음부터 안정적으로 써온 파일 저장 방식(`downloadBlob`)을 그대로 사용합니다. 받은 파일은 카카오톡의 "파일 첨부"로 보내면 됩니다.

### ③ 공유 기능 진단 화면에 한계 안내 추가

진단의 "성공"은 브라우저가 예외 없이 공유 인텐트를 넘겼다는 뜻일 뿐, 받는 앱이 실제로 내용을 보여줬는지는 알려주지 못합니다(이번 문제가 정확히 이 경우였습니다 — 오류는 없었는데 내용이 없었음). 이 한계를 진단 화면에 안내 문구로 명시하고, CSV·HTML·백업 테스트도 실제 공유 코드와 동일한 새 MIME으로 시험하도록 맞췄습니다.

## 4. 확인이 필요한 내용 (중요도 순)

1. 이번 버전으로 "CSV로 공유"를 다시 시도했을 때 카카오톡에 실제로 내용이 보이는 파일로 도착하는지 — **가장 중요합니다. 이번 조치가 맞았는지 틀렸는지를 가르는 확인입니다.**
2. 그래도 안 된다면, 새로 생긴 "💾 CSV 다운로드" 버튼으로 파일이 정상적으로 저장되고 여는지
3. 2번도 안 된다면: 다운로드 자체가 시작되지 않는지, 아니면 다운로드된 파일을 열었을 때 내용이 비어 있는지 (완전히 다른 문제일 수 있어 구분이 필요합니다)

## 5. 미구현 / 한계

- 이번 MIME 변경이 실제 원인이라는 것은 실기기 확인 전까지 추정입니다. 안드로이드/카카오톡의 내부 동작은 앱 코드만으로 완전히 통제할 수 없습니다.
- "다운로드도 안 된다"는 신고는 이번 버전에서 원인을 특정하지 못했고, CSV 전용 다운로드 버튼을 새로 만드는 것으로 우선 대응했습니다. 4번 확인 결과에 따라 다음 버전에서 추가 조치가 필요할 수 있습니다.

## 데이터 안전성

저장 데이터 구조 변경 없음(DB_VERSION 39 유지). 기존 기록은 그대로입니다.

## 테스트 범위

실제로 수행한 테스트: 전체 스크립트 구문 검사(node --check), MIME 우선순위·대체(fallback) 분기 로직을 이번에 바뀐 코드와 동일한 조건으로 격리 테스트(정상 수용/거부 시 대체/거부 무관한 형식 영향 없음/둘 다 거부되면 안전하게 실패 처리 등 7개 케이스 모두 통과), 실제 ExcelJS로 위험성평가 구조의 워크북을 만들어 workbookToCsv가 여전히 정상적으로 실제 내용을 생성하는지 재확인.

확인하지 못한 것: 이번 MIME 변경이 실제로 카카오톡에서 공유 성공률을 높이는지는 이 세션에 실기기가 없어 확인하지 못했습니다. 이것이 근본 원인이 아니었을 가능성도 있다는 점을 다시 한번 명확히 말씀드립니다 — 그래서 원인과 무관하게 항상 동작하는 "CSV 다운로드" 버튼을 함께 넣었습니다.
